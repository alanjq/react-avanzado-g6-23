import React from "react";
import {Container} from '@mui/material'

export const Header = () => {
    
    return (
        <Container fixed>
            Shop
        </Container>
    )
}

export default Header
