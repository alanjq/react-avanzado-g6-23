import { useState } from "react"

const useStripe = () => {
    const [products, setProducts] = useState([])

    // POST al carrito en Stripe
    
    // GET para obtener items del carrito
    // state para items

    return { products }
}
export default useStripe
